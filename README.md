# Anaya

Softsove click-ops dashboard (Next.js).

## Local

```bash
npm install
cp .env.example .env.local
npm run dev
```

Set `NEXT_PUBLIC_API_URL` to your Laravel API, e.g. `http://127.0.0.1:8000/api`.

On Vercel, add the same env var pointing at the live API URL.
